2018-03-18: The link /home/plariv/config.php exists to support an access to the Moodle config.php file that occurs under the "User Restore -> retrieve log" function. Because the customized modules are homed in /home/plariv/, the relative reference used in the submodule custom_mods/userrestore/view/log.php doesn't resolve as expected, so the link is a short-term hack to make the function operational. Whe the WBLMS is relocated on a new/different host, the custom mods deployment will deploy them into the Moodle sub-tree correctly, via automation. Until then, this hack will do...
